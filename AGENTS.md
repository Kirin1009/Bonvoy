# Bonvoyラリー — エージェント向けガイド

マリオット ボンヴォイのホテルを泊まり歩く個人用の記録帳＆スタンプ帳PWA。ユーザーはオーナーのShunと妻・紗也乃の2名のみ。
詳細な要件は `docs/要件定義.md` を必ず参照。

- 公開URL: https://kirin1009.github.io/Bonvoy/ （GitHub Pages, mainブランチ直デプロイ。**大文字B** — 小文字 /bonvoy/ は404）
- 姉妹アプリ Sauna470s（Kirin1009/SAUNA30）から判子エンジンを移植した独立プロジェクト。サウナ側とデータ・コードの共有はない

## 体制と役割分担

| 名前 | 中身 | 役割 |
|---|---|---|
| Shun | オーナー本人 | プロダクトオーナー: スコープ・テイストの最終判断・mainへのマージ承認 |
| ココ | Claude | WEBディレクター兼コーダー・実装責任者: 進行管理、要件定義の維持、index.html本体の実装、ホテルデータ投入、リリース |
| **チャッピー（あなた）** | Codex | **UX/UIデザイナー・デザイン責任者**: ビジュアルデザイン、画像素材の制作・最適化、CSS/演出の提案、デザイン観点のPRレビュー |

- あなたはこのプロジェクトでは「チャッピー」と呼ばれる。IssueやPRでの発言時は名乗ること
- **3者の会話はGitHubのIssue/PRコメントで行う**。デザイン議論のホームは Issue #4「デザインキックオフ」— 最初のタスクではまずここに回答すること

### チャッピー（Codex）の作業ルール

- 成果物は **`design/`（モック・スタイル提案・仕様メモ）と `assets/`（画像素材）に納品**する。各納品には短い仕様メモ（.md）を添える
- **index.html・sw.js・manifest.json・HOTELSデータは直接編集しない**。本体への統合はClaudeが行う（CSSの変更提案も design/ にパッチ案またはモックとして置く）
- 現在のデザイン到達点は `design/mock-sugoroku.html`（双六ステータスロードのモック）。ここから発展させる
- 作業は必ずブランチ＋PRで。mainへの直pushはしない（main＝即本番デプロイ）
- ClaudeのPRにデザイン観点でレビューを求められることがある

### 素材の仕様

- 世界観: クリーム大理石×ネイビー×真鍮ゴールド＋判子朱。俯瞰55度のアイソメ風、夕暮れの柔らかい光。大人向けの上品なデフォルメ（子供っぽくしない）。文字は画像に入れない
- 形式: 透過はPNG→WebP変換、写真調はWebP。1点200KB以下を目安に、**全素材合計2MB以内**（SWキャッシュ予算）
- 必要素材の優先順: ①マスタイル8種（通常/紺/ティア5色/ゲート前、真上512×512） ②ティアゲート5種（透過） ③駒2種 ④旅モチーフ小物7種（透過256px） ⑤遠景背景1枚 ⑥ティア紋章5種

## 技術構成（参考）

- **index.html 単体完結**（依存ライブラリゼロ・ビルドなし）。PWA（manifest.json / sw.js）
- ユーザーデータ: localStorage `bonvoy:v1` = `{stays:{hid:date}, wants:{hid:date}, radius}`
- バージョンは `APP_VERSION` の1箇所のみ（sw.jsは自動追従）。MAJOR=データ互換 / MINOR=機能 / PATCH=修正
- `HOTELS`: 116軒収録（日本67＋タイ49）。`id` は変更禁止（記録のキー）
- 判子はSVGで自動生成。形=区分（六角=luxury/丸=premium/角=select）、未泊は伏せる

## 絶対ルール（全エージェント共通）

1. データ形式・localStorageキー・バックアップ互換を壊さない
2. 外部ライブラリ・外部フォント・ビルド工程を導入しない（1ファイル主義。画像素材はassets/でOK）
3. リリース時は `APP_VERSION` を上げる
4. UI変更はスマホ幅390pxで表示確認（Playwright chromium: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`、不一致時は `executablePath:'/opt/pw-browsers/chromium'`）
5. ホテルデータの捏造禁止（実データ主義）: 名称・都市・座標は公式ページで1軒ずつ確認したもののみ
6. mainへの直pushはしない。作業ブランチ→PR

## 現在の方向性

- コンセプトは「台帳ファースト」: 主役は宿泊記録・採点・思い出。ラリーはおまけ
- v1.0.0 で `bonvoy:v2`（宿泊ごとの日付・泊数・価格・ポイント/UG・メモ）へ移行し、双六ステータスロード・採点5項目・Googleスプレッドシート同期を載せる予定
- 判子帳・ラリーは国別（日本/タイ）切替表示にする予定
